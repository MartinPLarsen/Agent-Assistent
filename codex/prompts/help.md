List the slash commands available in this agent and what they do.

Format the response as a markdown table with columns: Command | Purpose.

Read all files in `~/.codex/prompts/` and use each file's first sentence as the Purpose. Do not invent commands.
