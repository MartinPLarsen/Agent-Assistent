The user wants to recall something from memory. Steps:

1. Read `memory/MEMORY.md` and find index entries whose description matches their question.
2. Open only those fact files under `memory/facts/`. Do not read the whole folder.
3. If nothing in the index matches, search `memory/session.log.md` and `memory/convo_log.md` for recent context.
4. Report findings as bullets, each citing the file it came from.
5. Before recommending anything a memory names — a file, a flag, an endpoint — verify it still exists.
6. If nothing is found, say so. Do not invent.
