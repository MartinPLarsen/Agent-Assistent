Report the assistant's current status. Include:

1. Setup state from `.setup-state.json` — which phase, or done
2. Number of active items in `memory/open_commitments.md`
3. Number of facts in `memory/facts/` and the size of `memory/MEMORY.md`
4. Most recent entry in `memory/session.log.md`
5. Which watchers are enabled in `agent.json`, and any whose source is missing

Keep it under 8 lines.
